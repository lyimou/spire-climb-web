# 爬塔之旅 (Spire Climb)

A turn-based **deck-building roguelike** that runs entirely in the browser — no build step, no backend, no dependencies to install.

Pick a hero, climb a procedurally laid-out floor map, and fight turn-based battles with a deck you build as you go.

> **Note on the name:** this project is a web card-battler (a *Slay the Spire*-style deckbuilder), not a tower-defense game. The repository was renamed to match what the code actually implements.

---

## Preview

<!-- TODO: add screenshots here, e.g. ![map](screenshots/map.png) ![battle](screenshots/battle.png) -->

*No screenshots yet — the game is playable as-is; open `index.html` to see it.*

## Live demo

To run locally, just open `index.html` — or serve the folder:

```bash
python -m http.server 8000
# then open http://localhost:8000
```

## Features

- **Three playable heroes**, each with a different starting HP and relic (`铁甲战士` / `静默猎手` / `故障机器人`)
- **Floor-based map progression** with a progress indicator (elite / boss tracking)
- **Turn-based card combat**: energy per turn, block that expires at end of turn, damage resolution, and a live battle log
- **Deck management**: draw pile, hand (max 10), discard pile, and end-turn flow
- **Card detail modal** and card types (attack / skill / power)
- **Meta screens**: settings (fast mode, damage numbers, animation speed), in-game guide, character select
- **Data-driven card definitions** in `assets/data/cards.json`
- **Responsive, mobile-friendly layout** (`viewport` tuned, touch-friendly controls)
- **Chinese UI** with `Noto Sans SC` typography
- **Settings & persistence-free by design**: fast mode, damage numbers and animation speed are in-game options; nothing is sent anywhere and there is no backend call in the codebase

## Tech stack

| Layer | Choice |
| --- | --- |
| Language | Vanilla JavaScript (ES6+), no framework |
| Markup / styling | HTML5, CSS3 (custom properties, flex/grid) |
| Game data | JSON (`assets/data/cards.json`) |
| Icons / fonts | Font Awesome 6 (CDN), Google Fonts (CDN) |
| Build tooling | None — static files |

## Project structure

```
spire-climb-web/
├── index.html                  # entry point + all screens (map, battle, modals)
├── assets/
│   ├── style.css               # all styling
│   ├── game.js                 # game state machine, combat, map, deck logic
│   └── data/
│       └── cards.json          # card definitions
└── screenshots/
    └── demo.png                # placeholder
```

## How it works

- `index.html` declares every view (`#map-view`, `#battle-view`, start screen, settings/help modals) and the static skeleton; dynamic content (map nodes, hand cards, enemy info) is generated by `game.js`.
- `assets/game.js` holds all state and rules: hero selection, floor progression, card draw/discard, energy and block accounting, enemy turns, and the battle log.
- `assets/data/cards.json` keeps card data separate from logic, so adding a card does not require touching the combat engine.

## What I learned / challenges

> First-pass notes drawn from what the code actually does. Replace or expand these with your own account later.

- **State management without a framework**: keeping one source of truth for HP, energy, block, deck and map position, then re-rendering only the affected parts of the DOM, is the core difficulty of a game UI with no framework behind it.
- **Turn resolution order**: block has to expire at end of turn, damage has to resolve after block, and the battle log has to stay readable — getting this order wrong is what makes combat *feel* broken even when the numbers are right.
- **Data-driven content**: keeping cards in `assets/data/cards.json` instead of hard-coding their effects made adding and rebalancing cards cheap, and kept the combat engine readable.
- **Modals and view switching**: most of the UI is a small number of views plus overlays, so a clear "which view is active" rule prevented a whole class of layout bugs.
- **Mobile-first layout**: touch-friendly controls and `maximum-scale=1.0` in the viewport meta mean the same layout has to work on a phone without hover.

## Attribution & license

- Game design inspired by *Slay the Spire* (Mega Crit). All code and content in this repository are original; no assets are copied from that game.
- Font Awesome and Google Fonts are loaded from public CDNs.
- Released under the [MIT License](LICENSE).
